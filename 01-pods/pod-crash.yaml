apiVersion: v1
kind: Pod
metadata:
  name: crash-pod
  labels:
    app: crash-demo
spec:
  containers:
    - name: crash-container
      image: busybox:1.36
      command:
        - /bin/sh
        - -c
        - |
          echo "Starting application..."
          echo "Simulating an error!"
          exit 1
